const upperCase = (string) => string.toUpperCase();

const firstCharacter = ([firstCharacter]) => firstCharacter;
// const secondFunction = (str) =>  str.charAt(0); (GABARITO)

const stringConcat = (str1, str2) => str1 + str2;
// const thirdFunction = (str1, str2) => str1.concat(str2); (GABARITO)

module.exports = { upperCase, firstCharacter, stringConcat };